<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.billmate">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_fridge"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_fridge"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RevertBill"></activity>
        <activity
            android:name=".CreateBill"
            android:label="@string/write_something"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".InviteActivity" />
        <activity android:name=".CreateNewGroup" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignIn"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>